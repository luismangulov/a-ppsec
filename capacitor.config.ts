import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.appsec.app',
  appName: 'app sec',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
